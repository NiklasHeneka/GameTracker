# Credentials — never commit
.env
.env.*
!.env.example

node_modules/
dist/
src-tauri/target/
src-tauri/gen/

.DS_Store
*.log

# Editor workspace
.idea/
.vscode/

# Local overrides of the bundled data files. The app reads them from next to
# .env, which during development is the repository root — ignored there only,
# so the shipped copies under src-tauri/ stay tracked.
/ps-store-queries.json
/sale-calendar.json
